const number = document.querySelector([name=number])

const binary = parseInt(number, 2)
const octal = parseInt(number, 8)
const decimal = parseInt(number, 10)
const hexa = parseInt(number, 16)

console.log(binary, octal, decimal, hexa)